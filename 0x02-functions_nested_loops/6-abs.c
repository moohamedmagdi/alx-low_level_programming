#include "main.h"
/**
 * _abs - a function computes the absolute value of an integer
 * @ab: integer inp
 * Return: absolute value for ab
 */
int _abs(int ab)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
