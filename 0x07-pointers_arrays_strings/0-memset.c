#include "main.h"

/**
 * _memset - for iro
 *
 * @s: point. of char
 * @b: data for edit
 * @n: ind.
 *
 * Return: *s
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int x;

	for (x = 0; n > 0; x++, i--)
	{
		s[x] = b;
	}
	return (s);
}
